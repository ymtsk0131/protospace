$('.proto-list .row').empty();
$('.proto-list .row').html("<%= j(render partial: 'prototype', collection: @prototypes) %>");
$('.pagination').html("<%= j(paginate(@prototypes, remote: true, params: {order_by: @order_by})) %>");
